---
import Layout from '../../../layouts/Layout.astro';
import { getDocBySlug } from '../../../data/navigation';

const doc = getDocBySlug('mcp');

if (!doc) {
	throw new Error('MCP doc metadata is missing');
}
---

<Layout title={`${doc.title} | 通俗版 Claude Code 文档`} description={doc.summary} pathname={doc.path}>
	<article class="space-y-8">
		<header class="card rounded-[32px] p-8 shadow-card">
			<p class="text-sm font-semibold uppercase tracking-[0.24em]" style="color: var(--muted);">Build with Claude Code</p>
			<h1 class="mt-3 font-display text-4xl font-semibold">MCP 模型上下文协议</h1>
			<p class="mt-4 max-w-3xl text-lg leading-8">{doc.summary}</p>
			<p class="mt-4 max-w-4xl leading-8" style="color: var(--muted);">
				原页来源：
				<a href="https://code.claude.com/docs/en/mcp" target="_blank" rel="noreferrer" class="underline decoration-dotted underline-offset-4">官方 MCP</a>
			</p>
		</header>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">一句人话</h2>
			<div class="mt-4 space-y-4 leading-8">
				<p>MCP 就是个标准接口，让 Claude 能连外部数据源和工具。像给帮工配外接设备：数据库、API、文件系统、第三方服务都能接。</p>
				<p>有了 MCP，Claude 不只能改你本地文件，还能查数据库、调 API、连 Slack、查 Jira 工单。</p>
			</div>
		</section>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">MCP 能干嘛</h2>
			<div class="mt-4 space-y-5 leading-8">
				<div class="rounded-[24px] border p-6" style="border-color: var(--line); background: var(--panel-strong);">
					<h3 class="text-lg font-semibold">数据库查询</h3>
					<p class="mt-3" style="color: var(--muted);">连 PostgreSQL、MySQL，直接查生产数据找问题。</p>
				</div>
				<div class="rounded-[24px] border p-6" style="border-color: var(--line); background: var(--panel-strong);">
					<h3 class="text-lg font-semibold">API 调用</h3>
					<p class="mt-3" style="color: var(--muted);">连内部 API、第三方服务，获取实时数据。</p>
				</div>
				<div class="rounded-[24px] border p-6" style="border-color: var(--line); background: var(--panel-strong);">
					<h3 class="text-lg font-semibold">工具集成</h3>
					<p class="mt-3" style="color: var(--muted);">连 Slack、Jira、GitHub，在对话里查工单、发通知、提 PR。</p>
				</div>
				<div class="rounded-[24px] border p-6" style="border-color: var(--line); background: var(--panel-strong);">
					<h3 class="text-lg font-semibold">文件系统</h3>
					<p class="mt-3" style="color: var(--muted);">访问远程文件、云存储、共享目录。</p>
				</div>
			</div>
		</section>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">怎么配置 MCP</h2>
			<div class="mt-4 space-y-4 leading-8">
				<p>MCP 服务器配置在 <code class="px-2 py-1 rounded bg-code">~/Library/Application Support/Claude/claude_desktop_config.json</code>（macOS）或对应系统目录。</p>
				<div class="mt-4 rounded-[24px] border p-6 font-mono text-sm overflow-x-auto" style="border-color: var(--line); background: var(--panel-strong);">
<pre><![CDATA[{
  "mcpServers": {
    "filesystem": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "/path/to/allowed/files"]
    },
    "slack": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-slack"],
      "env": {
        "SLACK_BOT_TOKEN": "xoxb-..."
      }
    }
  }
}]]></pre>
				</div>
			</div>
		</section>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">现成的 MCP 服务器</h2>
			<div class="mt-4 space-y-4 leading-8" style="color: var(--muted);">
				<p>官方和社区有很多现成的 MCP 服务器：</p>
				<ul class="list-disc list-inside space-y-2 mt-2">
					<li><code class="px-2 py-1 rounded bg-code">@modelcontextprotocol/server-filesystem</code> — 文件系统访问</li>
					<li><code class="px-2 py-1 rounded bg-code">@modelcontextprotocol/server-slack</code> — Slack 集成</li>
					<li><code class="px-2 py-1 rounded bg-code">@modelcontextprotocol/server-postgres</code> — PostgreSQL 数据库</li>
					<li><code class="px-2 py-1 rounded bg-code">@modelcontextprotocol/server-github</code> — GitHub API</li>
					<li><code class="px-2 py-1 rounded bg-code">@modelcontextprotocol/server-memory</code> — 持久记忆</li>
				</ul>
			</div>
		</section>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">自己写 MCP 服务器</h2>
			<div class="mt-4 space-y-4 leading-8">
				<p>可以用 Node.js、Python 等语言写自己的 MCP 服务器。官方有 SDK：</p>
				<div class="mt-4 rounded-[24px] border p-6 font-mono text-sm overflow-x-auto" style="border-color: var(--line); background: var(--panel-strong);">
<pre># Node.js
npm install @modelcontextprotocol/sdk

# Python
pip install mcp</pre>
				</div>
				<p style="color: var(--muted);">定义好工具、资源、提示词，就能让 Claude 调用你的自定义功能。</p>
			</div>
		</section>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">安全注意事项</h2>
			<div class="mt-4 space-y-4 leading-8">
				<p><strong>1. 最小权限</strong> — MCP 服务器只给必要的权限，别给太多。</p>
				<p><strong>2. 凭证管理</strong> — API key、数据库密码用环境变量或密钥管理工具，别硬编码。</p>
				<p><strong>3. 输入验证</strong> — MCP 服务器要验证 Claude 传来的参数，防止注入攻击。</p>
				<p><strong>4. 审计日志</strong> — 记录所有 MCP 调用，方便事后追溯。</p>
			</div>
		</section>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">最后一句土话</h2>
			<div class="mt-4 space-y-4 leading-8">
				<p>MCP 就是给 Claude 开外挂。接得好，效率翻倍；接不好，安全隐患大。</p>
				<p style="color: var(--muted);">建议先用现成的 MCP 服务器，摸清楚门道再自己写。</p>
			</div>
		</section>
	</article>
</Layout>
