---
import Layout from '../../../layouts/Layout.astro';
import { getDocBySlug } from '../../../data/navigation';

const doc = getDocBySlug('desktop');

if (!doc) {
	throw new Error('Desktop doc metadata is missing');
}
---

<Layout title={`${doc.title} | 通俗版 Claude Code 文档`} description={doc.summary} pathname={doc.path}>
	<article class="space-y-8">
		<header class="card rounded-[32px] p-8 shadow-card">
			<p class="text-sm font-semibold uppercase tracking-[0.24em]" style="color: var(--muted);">Platforms and integrations</p>
			<h1 class="mt-3 font-display text-4xl font-semibold">Claude Code on desktop 桌面端用</h1>
			<p class="mt-4 max-w-3xl text-lg leading-8">{doc.summary}</p>
			<p class="mt-4 max-w-4xl leading-8" style="color: var(--muted);">
				原页来源：
				<a href="https://code.claude.com/docs/en/desktop" target="_blank" rel="noreferrer" class="underline decoration-dotted underline-offset-4">官方 Claude Code on desktop</a>
			</p>
		</header>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">一句人话</h2>
			<div class="mt-4 space-y-4 leading-8">
				<p>桌面版就是把终端里的那个能干帮工，搬进一个看得见、点得着的图形界面里。</p>
				<p>像原来你是站在院子里喊师傅干活，现在变成墙上装了个控制面板：哪块地、哪种权限、改了哪几行、PR 过没过，一眼就看见。</p>
			</div>
		</section>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">它比纯终端多了啥</h2>
			<div class="mt-4 space-y-4 leading-8" style="color: var(--muted);">
				<p>官方列出来的加成很明确：可视化 diff、应用预览、GitHub PR 状态监控、并行会话自动 worktree 隔离、定时任务、还有 GitHub/Slack/Linear 这些 connectors。</p>
				<p>说白了，它像给拖拉机加了驾驶室、后视镜和仪表盘，活还是那台车干，但你盯起来更省劲。</p>
			</div>
		</section>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">开一局前要先定四件事</h2>
			<div class="mt-4 space-y-4 leading-8">
				<p>官方说，在你发第一句话前，最好先把这四样定好：</p>
				<p style="color: var(--muted);">1. 环境：本机 Local、云上 Remote，或者你自己管的 SSH 远端。</p>
				<p style="color: var(--muted);">2. 项目目录：Claude 到底在哪个文件夹里干活。</p>
				<p style="color: var(--muted);">3. 模型：会话一旦开了，模型就锁住了。</p>
				<p style="color: var(--muted);">4. 权限模式：让它每步都请示，还是只改文件不请示，还是先只出计划。</p>
			</div>
		</section>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">几个最好用的地方</h2>
			<div class="mt-4 space-y-4 leading-8">
				<p>第一是预览。官方说它能起 dev server、开内嵌浏览器、自己点页面、看截图、看 DOM，出毛病了还会继续修。</p>
				<p>第二是 diff review。改完以后你能一行一行看，直接在线上留言让它返工。</p>
				<p>第三是 PR 监控。开了 PR 后，它能盯 GitHub CI 结果，失败了自动尝试修，过了还能自动 squash merge。</p>
				<p>这就像你不只请了个干活的人，还请了个会验收、会盯工期、会追包工头的管事。</p>
			</div>
		</section>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">并行干活这块很强</h2>
			<div class="mt-4 space-y-4 leading-8">
				<p>官方说，桌面版支持多会话并行，而且 Git 仓库会自动用 worktree 隔开。</p>
				<p style="color: var(--muted);">也就是一件活一个小院子，各自改各自的，不会把同一把铁锹抢来抢去。默认这些 worktree 放在 <code>&lt;project-root&gt;/.claude/worktrees/</code>。</p>
				<p style="color: var(--muted);">如果你想让长活慢慢跑，还能直接选 Remote，让云上去跑。你把电脑合上，活也不断。</p>
			</div>
		</section>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">权限模式咋选</h2>
			<div class="mt-4 space-y-4 leading-8" style="color: var(--muted);">
				<p><code>Ask permissions</code>：新手最稳，改文件和跑命令都先问你。</p>
				<p><code>Auto accept edits</code>：文件改动自动过，但跑命令还会问。</p>
				<p><code>Plan mode</code>：只看现场、出计划，不下手。</p>
				<p><code>Bypass permissions</code>：完全不问，官方明确提醒只该在沙箱容器或虚拟机里用。</p>
			</div>
		</section>
	</article>
</Layout>
