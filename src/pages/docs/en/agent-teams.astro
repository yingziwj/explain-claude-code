---
import Layout from '../../../layouts/Layout.astro';
import { getDocBySlug } from '../../../data/navigation';

const doc = getDocBySlug('agent-teams');

if (!doc) {
	throw new Error('Agent teams doc metadata is missing');
}
---

<Layout title={`${doc.title} | 通俗版 Claude Code 文档`} description={doc.summary} pathname={doc.path}>
	<article class="space-y-8">
		<header class="card rounded-[32px] p-8 shadow-card">
			<p class="text-sm font-semibold uppercase tracking-[0.24em]" style="color: var(--muted);">Build with Claude Code</p>
			<h1 class="mt-3 font-display text-4xl font-semibold">Agent teams 运行代理团队</h1>
			<p class="mt-4 max-w-3xl text-lg leading-8">{doc.summary}</p>
			<p class="mt-4 max-w-4xl leading-8" style="color: var(--muted);">
				原页来源：
				<a href="https://code.claude.com/docs/en/agent-teams" target="_blank" rel="noreferrer" class="underline decoration-dotted underline-offset-4">官方 Agent teams</a>
			</p>
		</header>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">一句人话</h2>
			<div class="mt-4 space-y-4 leading-8">
				<p>代理团队就是让多个 Claude 实例并行干活，还能互相通信。像包工头带施工队，各干各的，但能互相配合。</p>
				<p>子代理是在一个会话里轮流干，代理团队是多个会话同时干。</p>
			</div>
		</section>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">啥时候用代理团队</h2>
			<div class="mt-4 space-y-4 leading-8">
				<p><strong>大规模探索</strong> — 同时分析多个模块或目录，比串行快得多。</p>
				<p><strong>多任务并发</strong> — 一边写代码、一边写测试、一边写文档，同时进行。</p>
				<p><strong>复杂项目分解</strong> — 把大任务拆成多个子任务，分给不同代理同时做。</p>
			</div>
		</section>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">和子代理的区别</h2>
			<div class="mt-4 space-y-4 leading-8" style="color: var(--muted);">
				<p><strong>子代理</strong> — 同一个会话里，Claude 内部切换。上下文共享，串行执行。</p>
				<p><strong>代理团队</strong> — 多个独立会话，每个有自己的上下文。可以真正并行，还能互相发消息。</p>
				<p>简单说：子代理是"一个人分饰多角"，代理团队是"一群人各演各的"。</p>
			</div>
		</section>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">基本用法</h2>
			<div class="mt-4 space-y-4 leading-8">
				<p>用 <code class="px-2 py-1 rounded bg-code">claude --agent</code> 启动主代理，然后在对话里用 <code class="px-2 py-1 rounded bg-code">Agent()</code> 工具创建团队成员。</p>
				<div class="mt-4 rounded-[24px] border p-6 font-mono text-sm overflow-x-auto" style="border-color: var(--line); background: var(--panel-strong);">
<pre># 启动主代理
claude --agent "协调多个子任务"

# 在对话里创建团队成员
Agent("researcher", "负责调研和收集信息")
Agent("implementer", "负责具体实现")
Agent("reviewer", "负责审查代码")</pre>
				</div>
			</div>
		</section>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">团队成员怎么通信</h2>
			<div class="mt-4 space-y-4 leading-8" style="color: var(--muted);">
				<p>团队成员可以通过共享目录、消息传递、或主代理协调来通信。</p>
				<p>比如：研究员把调研结果写到 <code class="px-2 py-1 rounded bg-code">.claude/team/research.md</code>，实现员读取这个文件获取上下文。</p>
			</div>
		</section>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">最后一句土话</h2>
			<div class="mt-4 space-y-4 leading-8">
				<p>代理团队适合大项目、多任务、需要真正并行的场景。小活路子代理就够了，大工程才需要拉团队。</p>
				<p style="color: var(--muted);">记住：团队越大，协调成本越高。别为了炫技拉一堆人，结果光开会不干活。</p>
			</div>
		</section>
	</article>
</Layout>
