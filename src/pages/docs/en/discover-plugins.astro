---
import Layout from '../../../layouts/Layout.astro';
import { getDocBySlug } from '../../../data/navigation';

const doc = getDocBySlug('discover-plugins');

if (!doc) {
	throw new Error('Discover plugins doc metadata is missing');
}
---

<Layout title={`${doc.title} | 通俗版 Claude Code 文档`} description={doc.summary} pathname={doc.path}>
	<article class="space-y-8">
		<header class="card rounded-[32px] p-8 shadow-card">
			<p class="text-sm font-semibold uppercase tracking-[0.24em]" style="color: var(--muted);">Build with Claude Code</p>
			<h1 class="mt-3 font-display text-4xl font-semibold">Discover plugins 发现和安装插件</h1>
			<p class="mt-4 max-w-3xl text-lg leading-8">{doc.summary}</p>
		</header>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">一句人话</h2>
			<div class="mt-4 space-y-4 leading-8">
				<p>不用重复造轮子。去插件市场找现成的，装上就能用。也能把自己写的分享出去。</p>
			</div>
		</section>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">去哪找插件</h2>
			<div class="mt-4 space-y-4 leading-8">
				<p><strong>npm</strong> — 搜索带 <code class="px-2 py-1 rounded bg-code">claude-code-plugin</code> 标签的包：</p>
				<div class="mt-4 rounded-[24px] border p-6 font-mono text-sm overflow-x-auto" style="border-color: var(--line); background: var(--panel-strong);">
<pre># npm 网站搜索
https://www.npmjs.com/search?q=claude-code-plugin

# 命令行搜索
npm search claude-code-plugin</pre>
				</div>

				<p><strong>GitHub</strong> — 搜索相关仓库：</p>
				<div class="mt-4 rounded-[24px] border p-6 font-mono text-sm overflow-x-auto" style="border-color: var(--line); background: var(--panel-strong);">
<pre># GitHub 搜索链接
https://github.com/search?q=claude-code+plugin&type=repositories</pre>
				</div>

				<p><strong>官方推荐</strong> — 查看 Anthropic 官方文档和博客</p>

				<p><strong>社区分享</strong> — Discord、Reddit、Twitter 等社区</p>
			</div>
		</section>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">常见插件类型</h2>
			<div class="mt-4 space-y-5 leading-8">
				<div class="rounded-[24px] border p-6" style="border-color: var(--line); background: var(--panel-strong);">
					<h3 class="text-lg font-semibold">代码审查类</h3>
					<p class="mt-3" style="color: var(--muted);">自动审查代码质量、安全性、最佳实践。</p>
					<p class="mt-2" style="color: var(--muted);">示例：<code class="px-2 py-1 rounded bg-code">@anthropic/code-reviewer</code></p>
				</div>
				<div class="rounded-[24px] border p-6" style="border-color: var(--line); background: var(--panel-strong);">
					<h3 class="text-lg font-semibold">测试生成类</h3>
					<p class="mt-3" style="color: var(--muted);">自动生成单元测试、集成测试。</p>
					<p class="mt-2" style="color: var(--muted);">示例：<code class="px-2 py-1 rounded bg-code">@anthropic/test-generator</code></p>
				</div>
				<div class="rounded-[24px] border p-6" style="border-color: var(--line); background: var(--panel-strong);">
					<h3 class="text-lg font-semibold">文档生成类</h3>
					<p class="mt-3" style="color: var(--muted);">自动生成 API 文档、README、注释。</p>
				</div>
				<div class="rounded-[24px] border p-6" style="border-color: var(--line); background: var(--panel-strong);">
					<h3 class="text-lg font-semibold">MCP 集成类</h3>
					<p class="mt-3" style="color: var(--muted);">提供数据库、API、第三方服务的 MCP 连接器。</p>
				</div>
			</div>
		</section>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">管理插件</h2>
			<div class="mt-4 space-y-4 leading-8">
				<p>用 <code class="px-2 py-1 rounded bg-code">claude plugins</code> 命令管理：</p>
				<div class="mt-4 rounded-[24px] border p-6 font-mono text-sm overflow-x-auto" style="border-color: var(--line); background: var(--panel-strong);">
<pre># 列出已安装插件
claude plugins list

# 安装插件
claude plugins install @name/plugin-name

# 查看插件详情
claude plugins info @name/plugin-name

# 更新单个插件
claude plugins update @name/plugin-name

# 更新所有插件
claude plugins update --all

# 卸载插件
claude plugins uninstall @name/plugin-name

# 禁用/启用插件（在设置里配置）
# ~/.claude/settings.json 或 .claude/settings.json</pre>
				</div>
			</div>
		</section>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">插件配置</h2>
			<div class="mt-4 space-y-4 leading-8">
				<p>在设置文件里配置插件：</p>
				<div class="mt-4 rounded-[24px] border p-6 font-mono text-sm overflow-x-auto" style="border-color: var(--line); background: var(--panel-strong);">
<pre><![CDATA[{
  "plugins": {
    "@name/plugin-name": {
      "enabled": true,
      "config": {
        "apiKey": "your-api-key",
        "endpoint": "https://api.example.com"
      }
    }
  }
}]]></pre>
				</div>
			</div>
		</section>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">注意事项</h2>
			<div class="mt-4 space-y-4 leading-8" style="color: var(--muted);">
				<p><strong>1. 安全性</strong> — 只安装可信来源的插件，检查 package.json 和代码。</p>
				<p><strong>2. 权限</strong> — 插件可能请求文件访问、网络访问等权限，仔细审查。</p>
				<p><strong>3. 依赖冲突</strong> — 多个插件可能依赖不同版本的同一个包。</p>
				<p><strong>4. 性能影响</strong> — 安装太多插件可能影响启动速度和内存占用。</p>
				<p><strong>5. 定期更新</strong> — 保持插件更新，获取安全修复和新功能。</p>
			</div>
		</section>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">最后一句土话</h2>
			<div class="mt-4 space-y-4 leading-8">
				<p>插件市场就是"应用商店"，先找现成的，没有再自己写。</p>
			</div>
		</section>
	</article>
</Layout>
