---
import Layout from '../../../layouts/Layout.astro';
import { getDocBySlug } from '../../../data/navigation';

const doc = getDocBySlug('discover-plugins');

if (!doc) {
	throw new Error('Discover plugins doc metadata is missing');
}
---

<Layout title={`${doc.title} | 通俗版 Claude Code 文档`} description={doc.summary} pathname={doc.path}>
	<article class="space-y-8">
		<header class="card rounded-[32px] p-8 shadow-card">
			<p class="text-sm font-semibold uppercase tracking-[0.24em]" style="color: var(--muted);">Build with Claude Code</p>
			<h1 class="mt-3 font-display text-4xl font-semibold">Discover plugins 发现和安装插件</h1>
			<p class="mt-4 max-w-3xl text-lg leading-8">{doc.summary}</p>
		</header>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">一句人话</h2>
			<div class="mt-4 space-y-4 leading-8">
				<p>不用重复造轮子。去插件市场找现成的，装上就能用。也能把自己写的分享出去。</p>
			</div>
		</section>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">去哪找插件</h2>
			<div class="mt-4 space-y-4 leading-8" style="color: var(--muted);">
				<p><strong>npm</strong> — 搜索 <code class="px-2 py-1 rounded bg-code">claude-code-plugin</code> 标签的包</p>
				<p><strong>GitHub</strong> — 搜索 <code class="px-2 py-1 rounded bg-code">claude-code</code> 相关仓库</p>
				<p><strong>官方推荐</strong> — 查看 Anthropic 官方推荐的插件列表</p>
			</div>
		</section>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">管理插件</h2>
			<div class="mt-4 space-y-4 leading-8">
				<p>用 <code class="px-2 py-1 rounded bg-code">claude plugins</code> 命令管理：</p>
				<div class="mt-4 rounded-[24px] border p-6 font-mono text-sm overflow-x-auto" style="border-color: var(--line); background: var(--panel-strong);">
<pre># 列出已安装插件
claude plugins list

# 安装插件
claude plugins install @name/plugin-name

# 卸载插件
claude plugins uninstall @name/plugin-name

# 更新插件
claude plugins update</pre>
				</div>
			</div>
		</section>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">最后一句土话</h2>
			<div class="mt-4 space-y-4 leading-8">
				<p>插件市场就是"应用商店"，先找现成的，没有再自己写。</p>
			</div>
		</section>
	</article>
</Layout>
