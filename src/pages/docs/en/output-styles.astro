---
import Layout from '../../../layouts/Layout.astro';
import { getDocBySlug } from '../../../data/navigation';

const doc = getDocBySlug('output-styles');

if (!doc) {
	throw new Error('Output styles doc metadata is missing');
}
---

<Layout title={`${doc.title} | 通俗版 Claude Code 文档`} description={doc.summary} pathname={doc.path}>
	<article class="space-y-8">
		<header class="card rounded-[32px] p-8 shadow-card">
			<p class="text-sm font-semibold uppercase tracking-[0.24em]" style="color: var(--muted);">Build with Claude Code</p>
			<h1 class="mt-3 font-display text-4xl font-semibold">Output styles 输出风格</h1>
			<p class="mt-4 max-w-3xl text-lg leading-8">{doc.summary}</p>
			<p class="mt-4 max-w-4xl leading-8" style="color: var(--muted);">
				原页来源：
				<a href="https://code.claude.com/docs/en/output-styles" target="_blank" rel="noreferrer" class="underline decoration-dotted underline-offset-4">官方 Output styles</a>
			</p>
		</header>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">一句人话</h2>
			<div class="mt-4 space-y-4 leading-8">
				<p>输出风格就是控制 Claude 回答成什么样：纯文本、Markdown、JSON、XML 随便选。</p>
				<p>不同场景需要不同格式：写文档用 Markdown，程序解析用 JSON，正式报告用 XML。</p>
			</div>
		</section>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">怎么指定输出格式</h2>
			<div class="mt-4 space-y-4 leading-8">
				<p>直接在提示里说：</p>
				<div class="mt-4 rounded-[24px] border p-6 font-mono text-sm overflow-x-auto" style="border-color: var(--line); background: var(--panel-strong);">
<pre># 要 JSON
"用 JSON 格式输出，包含 title、content、tags 字段"

# 要 Markdown
"用 Markdown 格式写文档，带标题和代码块"

# 要纯文本
"用纯文本回答，不要任何格式"

# 要 XML
"用 XML 格式输出，根元素是 response"</pre>
				</div>
			</div>
		</section>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">常见输出格式</h2>
			<div class="mt-4 space-y-5 leading-8">
				<div class="rounded-[24px] border p-6" style="border-color: var(--line); background: var(--panel-strong);">
					<h3 class="text-lg font-semibold">纯文本</h3>
					<p class="mt-3" style="color: var(--muted);">适合简单回答、日志、命令行输出。</p>
				</div>
				<div class="rounded-[24px] border p-6" style="border-color: var(--line); background: var(--panel-strong);">
					<h3 class="text-lg font-semibold">Markdown</h3>
					<p class="mt-3" style="color: var(--muted);">适合文档、README、技术文章。支持标题、列表、代码块。</p>
				</div>
				<div class="rounded-[24px] border p-6" style="border-color: var(--line); background: var(--panel-strong);">
					<h3 class="text-lg font-semibold">JSON</h3>
					<p class="mt-3" style="color: var(--muted);">适合程序解析、API 响应、配置文件。</p>
				</div>
				<div class="rounded-[24px] border p-6" style="border-color: var(--line); background: var(--panel-strong);">
					<h3 class="text-lg font-semibold">XML</h3>
					<p class="mt-3" style="color: var(--muted);">适合正式文档、SOAP API、旧系统集成。</p>
				</div>
				<div class="rounded-[24px] border p-6" style="border-color: var(--line); background: var(--panel-strong);">
					<h3 class="text-lg font-semibold">HTML</h3>
					<p class="mt-3" style="color: var(--muted);">适合网页内容、邮件模板。</p>
				</div>
			</div>
		</section>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">输出技巧</h2>
			<div class="mt-4 space-y-4 leading-8" style="color: var(--muted);">
				<p><strong>1. 明确指定格式</strong> — 直接说"用 JSON 格式"，别含糊。</p>
				<p><strong>2. 给示例</strong> — 给个示例格式，Claude 更容易理解。</p>
				<p><strong>3. 定义结构</strong> — 对 JSON/XML，明确说清楚字段名和类型。</p>
				<p><strong>4. 验证输出</strong> — 程序解析前，先验证格式是否正确。</p>
			</div>
		</section>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">最后一句土话</h2>
			<div class="mt-4 space-y-4 leading-8">
				<p>输出格式就是说清楚你要啥样的回答。说清楚了，Claude 就给得准；说不清楚，它就瞎猜。</p>
				<p style="color: var(--muted);">建议常用格式写个模板，每次直接套用。</p>
			</div>
		</section>
	</article>
</Layout>
