---
import Layout from '../../../layouts/Layout.astro';
import { getDocBySlug } from '../../../data/navigation';

const doc = getDocBySlug('google-vertex-ai');

if (!doc) {
	throw new Error('Google Vertex AI doc metadata is missing');
}
---

<Layout title={`${doc.title} | 通俗版 Claude Code 文档`} description={doc.summary} pathname={doc.path}>
	<article class="space-y-8">
		<header class="card rounded-[32px] p-8 shadow-card">
			<p class="text-sm font-semibold uppercase tracking-[0.24em]" style="color: var(--muted);">Deployment</p>
			<h1 class="mt-3 font-display text-4xl font-semibold">Google Vertex AI</h1>
			<p class="mt-4 max-w-3xl text-lg leading-8">{doc.summary}</p>
			<p class="mt-4 max-w-4xl leading-8" style="color: var(--muted);">
				原页来源：
				<a href="https://code.claude.com/docs/en/google-vertex-ai" target="_blank" rel="noreferrer" class="underline decoration-dotted underline-offset-4">官方 Google Vertex AI</a>
			</p>
		</header>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">一句人话</h2>
			<div class="mt-4 space-y-4 leading-8">
				<p>Google Vertex AI 就是 GCP 版的 Claude Code。用 GCP 账号登录，走 GCP 计费，跑在 Google 云上。</p>
				<p>适合已经在用 GCP 生态的团队，统一账号、统一计费、统一权限管理。</p>
			</div>
		</section>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">啥时候选 Vertex AI</h2>
			<div class="mt-4 space-y-4 leading-8">
				<p><strong>1. 已经在用 GCP</strong> — 账号、计费、权限都在 GCP 里，不用另外搞一套。</p>
				<p><strong>2. 数据驻留要求</strong> — 数据必须留在 GCP 环境内。</p>
				<p><strong>3. 企业管控</strong> — 要用 GCP IAM 做权限管理，用 Cloud Audit Logs 做审计。</p>
				<p><strong>4. 成本优化</strong> — GCP 里有承诺用量折扣。</p>
			</div>
		</section>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">怎么配置</h2>
			<div class="mt-4 space-y-4 leading-8">
				<p>设置环境变量启用 Vertex AI：</p>
				<div class="mt-4 rounded-[24px] border p-6 font-mono text-sm overflow-x-auto" style="border-color: var(--line); background: var(--panel-strong);">
<pre># 启用 Vertex AI
export CLAUDE_CODE_USE_VERTEX=1
export CLOUD_ML_REGION=us-east5
export ANTHROPIC_VERTEX_PROJECT_ID=your-project-id

# 确保已配置 GCP 凭证
# gcloud auth application-default login</pre>
				</div>
			</div>
		</section>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">和直接用的区别</h2>
			<div class="mt-4 space-y-4 leading-8" style="color: var(--muted);">
				<p><strong>计费</strong> — Vertex AI 按 GCP 定价，走 GCP 账单。</p>
				<p><strong>模型</strong> — Vertex AI 上的模型版本可能比 Anthropic 官网慢半拍更新。</p>
				<p><strong>网页版</strong> — Vertex AI 不包含 Claude 网页版访问。</p>
			</div>
		</section>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">最后一句土话</h2>
			<div class="mt-4 space-y-4 leading-8">
				<p>Vertex AI 就是 GCP 生态里的 Claude Code。已经在用 GCP 就选它，不在 GCP 生态就直接用 Claude.com。</p>
			</div>
		</section>
	</article>
</Layout>
