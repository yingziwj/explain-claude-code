---
import Layout from '../../../layouts/Layout.astro';
import { getDocBySlug } from '../../../data/navigation';

const doc = getDocBySlug('setup');

if (!doc) {
	throw new Error('Administration doc metadata is missing');
}
---

<Layout title={`${doc.title} | 通俗版 Claude Code 文档`} description={doc.summary} pathname={doc.path}>
	<article class="space-y-8">
		<header class="card rounded-[32px] p-8 shadow-card">
			<p class="text-sm font-semibold uppercase tracking-[0.24em]" style="color: var(--muted);">Administration</p>
			<h1 class="mt-3 font-display text-4xl font-semibold">Administration 管理员设置</h1>
			<p class="mt-4 max-w-3xl text-lg leading-8">{doc.summary}</p>
			<p class="mt-4 max-w-4xl leading-8" style="color: var(--muted);">
				原页来源：
				<a href="https://code.claude.com/docs/en/setup" target="_blank" rel="noreferrer" class="underline decoration-dotted underline-offset-4">官方 Setup</a>
			</p>
		</header>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">一句人话</h2>
			<div class="mt-4 space-y-4 leading-8">
				<p>这一页主要不是给普通使用者看的，而是给“管钥匙、管大门、管规矩”的人看的。</p>
				<p>像村里新请来一批帮工，管理员不能只说“大家去干活吧”，还得先把门禁、仓库、工具、道路都铺好，不然下面的人一开工就卡住。</p>
			</div>
		</section>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">官方这页主要在讲什么</h2>
			<div class="mt-4 space-y-4 leading-8" style="color: var(--muted);">
				<p>1. 怎样做高级安装，比如打包分发、静默安装、预装到开发机里。</p>
				<p>2. 怎样给团队配置网络和代理，别让它一出门就撞墙。</p>
				<p>3. 怎样准备凭证、权限和组织级规则，让大家登录和使用都不乱套。</p>
				<p>4. 怎样在企业环境里把更新、审计、默认设置这些事情收拢起来管。</p>
			</div>
		</section>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">管理员最常要铺的几条路</h2>
			<div class="mt-4 space-y-5 leading-8">
				<div class="rounded-[24px] border p-6" style="border-color: var(--line); background: var(--panel-strong);">
					<h3 class="text-lg font-semibold">安装这条路</h3>
					<p class="mt-3" style="color: var(--muted);">不是让每个人都手工折腾一遍，而是想办法统一装好。比如通过脚本、系统镜像、包管理器，或者 IT 现成的软件分发渠道。</p>
				</div>
				<div class="rounded-[24px] border p-6" style="border-color: var(--line); background: var(--panel-strong);">
					<h3 class="text-lg font-semibold">联网这条路</h3>
					<p class="mt-3" style="color: var(--muted);">很多公司机器出网受管制，所以常要配 HTTP/HTTPS 代理、证书信任、白名单。不先打通，用户那边看着就像“工具装好了但喊不应”。</p>
				</div>
				<div class="rounded-[24px] border p-6" style="border-color: var(--line); background: var(--panel-strong);">
					<h3 class="text-lg font-semibold">认证这条路</h3>
					<p class="mt-3" style="color: var(--muted);">到底是用 Claude 账号、Claude Console，还是企业云平台接入，管理员得先定好。不同路子，对密钥、配额、审计的要求不一样。</p>
				</div>
			</div>
		</section>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">为什么官方把这页叫 Setup</h2>
			<div class="mt-4 space-y-4 leading-8">
				<p>因为对管理员来说，真正的难点常常不是“命令怎么敲”，而是“环境怎么铺平”。</p>
				<p>普通用户看到的是一个命令行；管理员看到的是一整套前置条件：机器上有没有 Node，网络通不通，证书认不认，自动更新能不能放行，日志怎么审，默认规则放哪儿。</p>
				<p style="color: var(--muted);">这些事做好了，底下的人会觉得 Claude Code 很顺；这些事没做好，底下的人只会觉得“这玩意儿总出毛病”。</p>
			</div>
		</section>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">管理员要特别盯住的坑</h2>
			<div class="mt-4 space-y-4 leading-8" style="color: var(--muted);">
				<p>1. 只发安装命令，不处理代理和证书，结果一堆人卡在登录和联网。</p>
				<p>2. 权限边界没讲清，导致有人以为能改仓库，其实只给了只读。</p>
				<p>3. 默认配置没统一，每台机器像各自搭棚子，问题很难排查。</p>
				<p>4. 更新策略没想好，要么版本太旧，要么突然升级把团队节奏打乱。</p>
			</div>
		</section>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">最后一句土话</h2>
			<div class="mt-4 space-y-4 leading-8">
				<p>管理员做的 setup，不是“帮大家装个软件”这么简单，而是先把院门、路灯、钥匙和规章全布置好。</p>
				<p style="color: var(--muted);">前面铺得平，后面用户才会觉得 Claude Code 是来干活的，不是来添堵的。</p>
			</div>
		</section>
	</article>
</Layout>
