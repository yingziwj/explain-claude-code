---
import Layout from '../../../layouts/Layout.astro';
import { getDocBySlug } from '../../../data/navigation';

const doc = getDocBySlug('quickstart');

if (!doc) {
	throw new Error('Quickstart doc metadata is missing');
}
---

<Layout title={`${doc.title} | 通俗版 Claude Code 文档`} description={doc.summary} pathname={doc.path}>
	<article class="space-y-8">
		<header class="card rounded-[32px] p-8 shadow-card">
			<p class="text-sm font-semibold uppercase tracking-[0.24em]" style="color: var(--muted);">Getting started</p>
			<h1 class="mt-3 font-display text-4xl font-semibold">Quickstart 快速上手</h1>
			<p class="mt-4 max-w-3xl text-lg leading-8">{doc.summary}</p>
			<p class="mt-4 max-w-4xl leading-8" style="color: var(--muted);">
				原页来源：
				<a href="https://code.claude.com/docs/en/quickstart" target="_blank" rel="noreferrer" class="underline decoration-dotted underline-offset-4">官方 Quickstart</a>
			</p>
		</header>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">一句人话</h2>
			<div class="mt-4 space-y-4 leading-8">
				<p>
					这一页讲的就是：别先研究一大堆原理，先把这个电子帮工领回家，给它开门、认路、交第一件活。
					像买了台新农机，先别急着琢磨齿轮怎么转，先学会加油、打火、开到地里试一圈。
				</p>
				<p>
					官方 Quickstart 的主线很直白：先准备家伙事，再安装、登录、进项目、问问题、改代码、配合 Git，
					最后再学几个常用口令和少走弯路的招数。
				</p>
			</div>
		</section>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">开工前，先把三样东西备齐</h2>
			<div class="mt-4 space-y-4 leading-8" style="color: var(--muted);">
				<p>1. 一个终端窗口。就像你得先把院门打开，帮工才能进来听活。</p>
				<p>2. 一个代码项目。总得有块地、有间房，它才知道去哪儿翻土、哪儿补墙。</p>
				<p>3. 能登录 Claude Code 的账号。官方写的是 Claude 订阅、Claude Console，或者接到支持的云服务上。</p>
				<p>
					这页主要教的是终端版。不过官方也特别提醒：网页端、桌面端、VS Code、JetBrains、Slack、GitHub Actions、GitLab
					这些地方也都能用，同一个电子帮工不只待在一个工棚里。
				</p>
			</div>
		</section>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">第一步：把帮工请进门</h2>
			<div class="mt-4 space-y-5 leading-8">
				<p>官方给了三种安装法。你不用全学，会一条顺手的就行。</p>
				<div class="rounded-[24px] border p-6" style="border-color: var(--line); background: var(--panel-strong);">
					<h3 class="font-semibold text-lg">原生安装，官方最推荐</h3>
					<div class="mt-3 space-y-4" style="color: var(--muted);">
						<p>macOS、Linux、WSL：</p>
						<pre class="overflow-x-auto rounded-2xl px-4 py-4 text-sm leading-7" style="background: var(--ink); color: var(--paper);"><code>curl -fsSL https://claude.ai/install.sh | bash</code></pre>
						<p>Windows PowerShell：</p>
						<pre class="overflow-x-auto rounded-2xl px-4 py-4 text-sm leading-7" style="background: var(--ink); color: var(--paper);"><code>irm https://claude.ai/install.ps1 | iex</code></pre>
						<p>Windows CMD：</p>
						<pre class="overflow-x-auto rounded-2xl px-4 py-4 text-sm leading-7" style="background: var(--ink); color: var(--paper);"><code>curl -fsSL https://claude.ai/install.cmd -o install.cmd &amp;&amp; install.cmd &amp;&amp; del install.cmd</code></pre>
						<p>官方还特别提醒：Windows 机器先装好 Git for Windows，不然像地里缺了扳手，后面不少活干不顺。</p>
						<p>原生安装会自动在后台更新，省得你老惦记着“这农具是不是又过期了”。</p>
					</div>
				</div>
				<div class="grid gap-4 md:grid-cols-2">
					<div class="rounded-[24px] border p-6" style="border-color: var(--line); background: var(--panel-strong);">
						<h3 class="font-semibold text-lg">Homebrew</h3>
						<div class="mt-3 space-y-3" style="color: var(--muted);">
							<pre class="overflow-x-auto rounded-2xl px-4 py-4 text-sm leading-7" style="background: var(--ink); color: var(--paper);"><code>brew install --cask claude-code</code></pre>
							<p>以后升级：</p>
							<pre class="overflow-x-auto rounded-2xl px-4 py-4 text-sm leading-7" style="background: var(--ink); color: var(--paper);"><code>brew upgrade claude-code</code></pre>
						</div>
					</div>
					<div class="rounded-[24px] border p-6" style="border-color: var(--line); background: var(--panel-strong);">
						<h3 class="font-semibold text-lg">WinGet</h3>
						<div class="mt-3 space-y-3" style="color: var(--muted);">
							<pre class="overflow-x-auto rounded-2xl px-4 py-4 text-sm leading-7" style="background: var(--ink); color: var(--paper);"><code>winget install Anthropic.ClaudeCode</code></pre>
							<p>以后升级：</p>
							<pre class="overflow-x-auto rounded-2xl px-4 py-4 text-sm leading-7" style="background: var(--ink); color: var(--paper);"><code>winget upgrade Anthropic.ClaudeCode</code></pre>
						</div>
					</div>
				</div>
			</div>
		</section>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">第二步：先认主，再干活</h2>
			<div class="mt-4 space-y-4 leading-8">
				<p>装完以后，先让它认你是谁。第一次最常见就是直接敲：</p>
				<pre class="overflow-x-auto rounded-2xl px-4 py-4 text-sm leading-7" style="background: var(--ink); color: var(--paper);"><code>claude</code></pre>
				<p style="color: var(--muted);">第一次运行时，系统会带你登录。你也可以先进程序后再敲下面这条：</p>
				<pre class="overflow-x-auto rounded-2xl px-4 py-4 text-sm leading-7" style="background: var(--ink); color: var(--paper);"><code>/login</code></pre>
				<p style="color: var(--muted);">
					官方列了几条登录路子：Claude Pro、Max、Teams、Enterprise 最省心；也能用 Claude Console，
					或者企业常见的 Amazon Bedrock、Google Vertex AI、Microsoft Foundry。
				</p>
				<p>你可以把这一步想成给帮工发门禁卡。卡没办好，再能干的人也进不了仓库。</p>
			</div>
		</section>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">第三步：带它进项目现场</h2>
			<div class="mt-4 space-y-4 leading-8">
				<p>别站在空地上瞎喊。先切到你的项目目录，再启动 Claude Code：</p>
				<pre class="overflow-x-auto rounded-2xl px-4 py-4 text-sm leading-7" style="background: var(--ink); color: var(--paper);"><code>cd /path/to/your/project
claude</code></pre>
				<p style="color: var(--muted);">
					这就像把帮工领到你那块地头，指着说：“活就在这儿，不是在隔壁村。”
				</p>
				<p>刚进去如果有点蒙，可以先敲 <code>/help</code> 看能用哪些口令；中途关了，下次还能用 <code>/resume</code> 接着干，不用从头再讲一遍。</p>
			</div>
		</section>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">第四步：先问路，别一上来就拆房子</h2>
			<div class="mt-4 space-y-4 leading-8" style="color: var(--muted);">
				<p>官方先让你问几个摸底问题，这个很对。新帮工到场，先让它认地形、认房梁、认水管。</p>
				<pre class="overflow-x-auto rounded-2xl px-4 py-4 text-sm leading-7" style="background: var(--ink); color: var(--paper);"><code>what does this project do?
what technologies does this project use?
where is the main entry point?
explain the folder structure</code></pre>
				<p>如果你还不确定这工具能干啥，也可以直接问：</p>
				<pre class="overflow-x-auto rounded-2xl px-4 py-4 text-sm leading-7" style="background: var(--ink); color: var(--paper);"><code>what can Claude Code do?
how do I create custom skills in Claude Code?
can Claude Code work with Docker?</code></pre>
				<p>这一步的目的不是闲聊，是让它先把院子看明白，后面动手才不容易砸错墙。</p>
			</div>
		</section>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">第五步：交第一件小活，让它先露一手</h2>
			<div class="mt-4 space-y-4 leading-8">
				<p>官方给的示例很简单，适合第一次试手：</p>
				<pre class="overflow-x-auto rounded-2xl px-4 py-4 text-sm leading-7" style="background: var(--ink); color: var(--paper);"><code>add a hello world function to the main file</code></pre>
				<p style="color: var(--muted);">
					按官方描述，它一般会先找对文件，再把改动方案给你看，问你同不同意，然后才真正下手去改。
					这就像靠谱的修房师傅，先指给你看“我要拆这块砖、补这道缝”，不会闷头一锤子抡下去。
				</p>
			</div>
		</section>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">第六步：让它学会跟 Git 一起干活</h2>
			<div class="mt-4 space-y-4 leading-8">
				<p>写代码不是单打独斗，很多活都离不开 Git。官方给了几句很实用的话：</p>
				<pre class="overflow-x-auto rounded-2xl px-4 py-4 text-sm leading-7" style="background: var(--ink); color: var(--paper);"><code>what files have I changed?
commit my changes with a descriptive message
create a new branch called feature/quickstart
show me the last 5 commits
help me resolve merge conflicts</code></pre>
				<p style="color: var(--muted);">
					你可以把 Git 想成工地账本。今天挪了哪块砖、昨天谁补了哪面墙、两个人同时改同一根梁怎么办，都记在这里。
				</p>
			</div>
		</section>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">第七步：开始修真毛病，或者加真功能</h2>
			<div class="mt-4 space-y-4 leading-8" style="color: var(--muted);">
				<p>等你确认它认路没问题，就可以给像样点的活：</p>
				<pre class="overflow-x-auto rounded-2xl px-4 py-4 text-sm leading-7" style="background: var(--ink); color: var(--paper);"><code>add input validation to the user registration form
there's a bug where users can submit empty forms - fix it</code></pre>
				<p>官方写得很实在：它会去找相关代码、理解上下文、想办法实现，再在有测试时顺手跑测试。</p>
				<p>这就不是“给你出主意”，而是真进屋查漏水、补裂缝、修完再开水龙头试一试。</p>
			</div>
		</section>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">第八步：把它当成能干杂活的熟练工</h2>
			<div class="mt-4 space-y-4 leading-8">
				<p>官方还举了几类常见活，你一看就明白范围不小：</p>
				<pre class="overflow-x-auto rounded-2xl px-4 py-4 text-sm leading-7" style="background: var(--ink); color: var(--paper);"><code>refactor the authentication module to use async/await instead of callbacks
write unit tests for the calculator functions
update the README with installation instructions
review my changes and suggest improvements</code></pre>
				<p style="color: var(--muted);">
					意思很简单：不只是修 bug，整理旧屋子、补说明书、写检查单、帮你看改动靠不靠谱，这些它都能搭把手。
				</p>
			</div>
		</section>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">几条最常用的口令，先记住这些就够用</h2>
			<div class="mt-6 overflow-x-auto">
				<table class="min-w-full text-left text-sm leading-7">
					<thead>
						<tr style="color: var(--muted);">
							<th class="px-4 py-3 font-semibold">命令</th>
							<th class="px-4 py-3 font-semibold">人话意思</th>
							<th class="px-4 py-3 font-semibold">例子</th>
						</tr>
					</thead>
					<tbody>
						<tr class="border-t" style="border-color: var(--line);">
							<td class="px-4 py-3"><code>claude</code></td>
							<td class="px-4 py-3">进入对话式干活模式</td>
							<td class="px-4 py-3"><code>claude</code></td>
						</tr>
						<tr class="border-t" style="border-color: var(--line);">
							<td class="px-4 py-3"><code>claude "task"</code></td>
							<td class="px-4 py-3">一次性派个活</td>
							<td class="px-4 py-3"><code>claude "fix the build error"</code></td>
						</tr>
						<tr class="border-t" style="border-color: var(--line);">
							<td class="px-4 py-3"><code>claude -p "query"</code></td>
							<td class="px-4 py-3">问完就走，不停留</td>
							<td class="px-4 py-3"><code>claude -p "explain this function"</code></td>
						</tr>
						<tr class="border-t" style="border-color: var(--line);">
							<td class="px-4 py-3"><code>claude -c</code></td>
							<td class="px-4 py-3">接着当前目录最近一次对话继续干</td>
							<td class="px-4 py-3"><code>claude -c</code></td>
						</tr>
						<tr class="border-t" style="border-color: var(--line);">
							<td class="px-4 py-3"><code>claude -r</code></td>
							<td class="px-4 py-3">恢复以前某次对话</td>
							<td class="px-4 py-3"><code>claude -r</code></td>
						</tr>
						<tr class="border-t" style="border-color: var(--line);">
							<td class="px-4 py-3"><code>claude commit</code></td>
							<td class="px-4 py-3">帮你生成 Git 提交</td>
							<td class="px-4 py-3"><code>claude commit</code></td>
						</tr>
						<tr class="border-t" style="border-color: var(--line);">
							<td class="px-4 py-3"><code>/clear</code></td>
							<td class="px-4 py-3">清掉当前对话上下文</td>
							<td class="px-4 py-3"><code>/clear</code></td>
						</tr>
						<tr class="border-t" style="border-color: var(--line);">
							<td class="px-4 py-3"><code>/help</code></td>
							<td class="px-4 py-3">查看可用命令</td>
							<td class="px-4 py-3"><code>/help</code></td>
						</tr>
						<tr class="border-t" style="border-color: var(--line);">
							<td class="px-4 py-3"><code>exit</code> 或 <code>Ctrl+C</code></td>
							<td class="px-4 py-3">退出 Claude Code</td>
							<td class="px-4 py-3"><code>exit</code></td>
						</tr>
					</tbody>
				</table>
			</div>
		</section>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">新手少走弯路的三句提醒</h2>
			<div class="mt-4 space-y-4 leading-8">
				<p>第一，话要说具体。别说“帮我弄一下”。要像请人种地那样说清楚：翻哪块地，种啥，先后顺序怎么来。</p>
				<pre class="overflow-x-auto rounded-2xl px-4 py-4 text-sm leading-7" style="background: var(--ink); color: var(--paper);"><code>1. create a new database table for user profiles
2. create an API endpoint to get and update user profiles
3. build a webpage that allows users to see and edit their information</code></pre>
				<p>第二，先让它勘察，再让它动手。官方示例里先看数据库结构，再做英国客户退货看板，这就比一上来乱改稳得多。</p>
				<pre class="overflow-x-auto rounded-2xl px-4 py-4 text-sm leading-7" style="background: var(--ink); color: var(--paper);"><code>analyze the database schema
build a dashboard showing products that are most frequently returned by our UK customers</code></pre>
				<p>第三，学会偷懒。按 <code>?</code> 看快捷键，按 <code>Tab</code> 自动补全，按 <code>↑</code> 找历史命令，输入 <code>/</code> 看全部命令和 skills。</p>
			</div>
		</section>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">看完这页，接着往哪走</h2>
			<div class="mt-4 space-y-4 leading-8" style="color: var(--muted);">
				<p>官方下一步推荐几页，顺序也挺合理。</p>
				<p>
					1. 看
					<a href="/docs/en/how-claude-code-works" class="underline decoration-dotted underline-offset-4">How Claude Code works</a>
					：弄明白这帮工为啥会这样看、这样改、这样问你确认。
				</p>
				<p>
					2. 看
					<a href="/docs/en/best-practices" class="underline decoration-dotted underline-offset-4">Best practices</a>
					：学会怎么交代活，别把好帮手使唤成瞎忙活。
				</p>
				<p>
					3. 看
					<a href="/docs/en/common-workflows" class="underline decoration-dotted underline-offset-4">Common workflows</a>
					和
					<a href="/docs/en/features-overview" class="underline decoration-dotted underline-offset-4">Extend Claude Code</a>
					：把常见套路和扩展玩法都补上。
				</p>
				<p>如果卡住了，官方建议直接在 Claude Code 里敲 <code>/help</code>，或者继续翻文档、进社区问人。</p>
			</div>
		</section>
	</article>
</Layout>
