---
import Layout from '../../../layouts/Layout.astro';
import { getDocBySlug } from '../../../data/navigation';

const doc = getDocBySlug('code-review');

if (!doc) {
	throw new Error('Code Review doc metadata is missing');
}
---

<Layout title={`${doc.title} | 通俗版 Claude Code 文档`} description={doc.summary} pathname={doc.path}>
	<article class="space-y-8">
		<header class="card rounded-[32px] p-8 shadow-card">
			<p class="text-sm font-semibold uppercase tracking-[0.24em]" style="color: var(--muted);">Code review &amp; CI/CD</p>
			<h1 class="mt-3 font-display text-4xl font-semibold">GitHub Code Review 代码审查</h1>
			<p class="mt-4 max-w-3xl text-lg leading-8">{doc.summary}</p>
			<p class="mt-4 max-w-4xl leading-8" style="color: var(--muted);">
				原页来源：
				<a href="https://code.claude.com/docs/en/code-review" target="_blank" rel="noreferrer" class="underline decoration-dotted underline-offset-4">官方 Code Review</a>
			</p>
		</header>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">一句人话</h2>
			<div class="mt-4 space-y-4 leading-8">
				<p>这功能不是替你写代码，而是像请了几个懂行师傅，专门在 PR 提交后帮你挑毛病。</p>
				<p>而且不是只看表面格式，官方重点说它盯的是逻辑错误、安全漏洞、回归问题这些“合不合格”的硬伤。</p>
			</div>
		</section>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">它怎么审</h2>
			<div class="mt-4 space-y-4 leading-8">
				<p>官方说背后不是单个 agent 看一眼，而是一组分工不同的 agent 并行分析 diff 和周边代码，再经过一道验证步骤，把假警报筛掉。</p>
				<p style="color: var(--muted);">最后结果会去重、按严重程度排好，直接作为行内评论贴在 PR 对应代码旁边。</p>
				<p>这就像不是找一个人拍脑袋验房，而是水电、泥工、木工分别看一遍，再由总工确认哪些真是问题。</p>
			</div>
		</section>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">会不会把你的流程搅乱</h2>
			<div class="mt-4 space-y-4 leading-8" style="color: var(--muted);">
				<p>官方特意说了：它不会替你 approve，也不会直接 block PR。</p>
				<p>也就是说，它像一个提前把关的老师傅，指出问题，但“准不准过、谁最后拍板”还是你们现有评审流程说了算。</p>
			</div>
		</section>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">它看重什么，不看重什么</h2>
			<div class="mt-4 space-y-4 leading-8">
				<p>默认重点是 correctness，也就是会不会真的出 bug、会不会把生产搞坏。</p>
				<p style="color: var(--muted);">官方明说，默认不主打格式偏好和缺测试覆盖这种东西。你想让它按你们自家规矩看，可以往仓库里加 <code>CLAUDE.md</code> 或 <code>REVIEW.md</code>。</p>
				<p style="color: var(--muted);">严重级别也分三档：Normal、Nit、Pre-existing。</p>
			</div>
		</section>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">怎么开通</h2>
			<div class="mt-4 space-y-4 leading-8">
				<p>这不是个人随手一开就有的功能。官方说它现在是 research preview，只给 Teams 和 Enterprise，而且 Zero Data Retention 开着的组织不能用。</p>
				<p style="color: var(--muted);">管理员得先去 <code>claude.ai/admin-settings/claude-code</code> 里开 Code Review，再安装 Claude GitHub App，给仓库权限，并给每个仓库设定 review 触发时机。</p>
			</div>
		</section>
	</article>
</Layout>
