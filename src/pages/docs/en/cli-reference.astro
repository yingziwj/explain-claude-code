---
import Layout from '../../../layouts/Layout.astro';
import { getDocBySlug } from '../../../data/navigation';

const doc = getDocBySlug('cli-reference');

if (!doc) {
	throw new Error('Reference doc metadata is missing');
}
---

<Layout title={`${doc.title} | 通俗版 Claude Code 文档`} description={doc.summary} pathname={doc.path}>
	<article class="space-y-8">
		<header class="card rounded-[32px] p-8 shadow-card">
			<p class="text-sm font-semibold uppercase tracking-[0.24em]" style="color: var(--muted);">Reference</p>
			<h1 class="mt-3 font-display text-4xl font-semibold">Reference 命令行指令对照表</h1>
			<p class="mt-4 max-w-3xl text-lg leading-8">{doc.summary}</p>
			<p class="mt-4 max-w-4xl leading-8" style="color: var(--muted);">
				原页来源：
				<a href="https://code.claude.com/docs/en/cli-reference" target="_blank" rel="noreferrer" class="underline decoration-dotted underline-offset-4">官方 CLI reference</a>
			</p>
		</header>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">一句人话</h2>
			<div class="mt-4 space-y-4 leading-8">
				<p>这一页就是命令行版说明书，告诉你每个口令、参数、开关大概是干嘛的。</p>
				<p>像农机说明书最后那几页：发动怎么发、挂档怎么挂、临时停机怎么停、哪些按钮是诊断用的，全列给你查。</p>
			</div>
		</section>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">先记住最常用的几条</h2>
			<div class="mt-4 overflow-x-auto">
				<table class="min-w-full text-left text-sm leading-7">
					<thead>
						<tr style="color: var(--muted);">
							<th class="px-4 py-3 font-semibold">命令</th>
							<th class="px-4 py-3 font-semibold">人话意思</th>
							<th class="px-4 py-3 font-semibold">什么时候用</th>
						</tr>
					</thead>
					<tbody>
						<tr class="border-t" style="border-color: var(--line);">
							<td class="px-4 py-3"><code>claude</code></td>
							<td class="px-4 py-3">正常进入交互式干活模式</td>
							<td class="px-4 py-3">坐下来慢慢聊、慢慢改时用</td>
						</tr>
						<tr class="border-t" style="border-color: var(--line);">
							<td class="px-4 py-3"><code>claude "任务内容"</code></td>
							<td class="px-4 py-3">直接带着一句任务启动</td>
							<td class="px-4 py-3">懒得先进去再敲字时用</td>
						</tr>
						<tr class="border-t" style="border-color: var(--line);">
							<td class="px-4 py-3"><code>claude -p "问题"</code></td>
							<td class="px-4 py-3">问完就出结果，不进长期会话</td>
							<td class="px-4 py-3">适合一次性查询、脚本里调用</td>
						</tr>
						<tr class="border-t" style="border-color: var(--line);">
							<td class="px-4 py-3"><code>claude -c</code></td>
							<td class="px-4 py-3">继续最近一次会话</td>
							<td class="px-4 py-3">上回干到一半，今天接着来</td>
						</tr>
						<tr class="border-t" style="border-color: var(--line);">
							<td class="px-4 py-3"><code>claude -r</code></td>
							<td class="px-4 py-3">恢复更早的某次会话</td>
							<td class="px-4 py-3">想翻旧账、接着老任务时用</td>
						</tr>
					</tbody>
				</table>
			</div>
		</section>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">参数这东西，别背全表，先懂分类</h2>
			<div class="mt-4 space-y-4 leading-8">
				<p>有的参数是管输入方式的，比如直接给 prompt，或者从脚本里喂内容。</p>
				<p>有的是管会话的，比如继续上次、恢复旧会话、开新会话。</p>
				<p>有的是管行为的，比如打印成什么格式、要不要静一点、是否进入某种模式。</p>
				<p style="color: var(--muted);">你不用第一次就把所有旗子认全，先知道它们分哪几类，查的时候就快很多。</p>
			</div>
		</section>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">斜杠命令和命令行参数，不是一回事</h2>
			<div class="mt-4 space-y-4 leading-8" style="color: var(--muted);">
				<p><code>claude -p</code> 这种，是你在外面启动程序时带的参数。</p>
				<p><code>/help</code>、<code>/clear</code>、<code>/resume</code> 这种，是你已经进到 Claude 里面以后，再对它下的口令。</p>
				<p>一个像“上车前告诉司机去哪里”，一个像“车开起来以后让司机拐弯、停车、继续走”。</p>
			</div>
		</section>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">官方参考页最适合拿来干嘛</h2>
			<div class="mt-4 space-y-4 leading-8">
				<p>1. 忘了某个参数怎么写时，快速查一下，不靠猜。</p>
				<p>2. 想把 Claude Code 塞进 shell 脚本、CI 流水线时，查哪些参数适合非交互场景。</p>
				<p>3. 想确认某个行为到底是启动参数控制，还是进入会话后用斜杠命令控制。</p>
			</div>
		</section>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">最后一句土话</h2>
			<div class="mt-4 space-y-4 leading-8">
				<p>CLI reference 不是让你当字典背下来，而是让你干活卡壳时，知道去哪一页翻。</p>
				<p style="color: var(--muted);">会用的人，脑子里记的是常用几条，剩下的靠对照表随查随用，这才省劲。</p>
			</div>
		</section>
	</article>
</Layout>
