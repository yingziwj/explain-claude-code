---
import Layout from '../../../layouts/Layout.astro';
import { getDocBySlug } from '../../../data/navigation';

const doc = getDocBySlug('remote-control');

if (!doc) {
	throw new Error('Remote Control doc metadata is missing');
}
---

<Layout title={`${doc.title} | 通俗版 Claude Code 文档`} description={doc.summary} pathname={doc.path}>
	<article class="space-y-8">
		<header class="card rounded-[32px] p-8 shadow-card">
			<p class="text-sm font-semibold uppercase tracking-[0.24em]" style="color: var(--muted);">Platforms and integrations</p>
			<h1 class="mt-3 font-display text-4xl font-semibold">Remote Control 远程使唤它</h1>
			<p class="mt-4 max-w-3xl text-lg leading-8">{doc.summary}</p>
			<p class="mt-4 max-w-4xl leading-8" style="color: var(--muted);">
				原页来源：
				<a href="https://code.claude.com/docs/en/remote-control" target="_blank" rel="noreferrer" class="underline decoration-dotted underline-offset-4">官方 Remote Control</a>
			</p>
		</header>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">一句人话</h2>
			<div class="mt-4 space-y-4 leading-8">
				<p>
					Remote Control 的意思，不是把活搬到云上去跑，而是让你用手机、平板、别人的电脑浏览器，去遥控你自己那台正在跑 Claude Code 的机器。
				</p>
				<p>
					像你人在地头外边，但家里那台拖拉机还在院里发动着。你不是又租了一台新车，而是拿了个远程对讲机，继续指挥原来那台车干活。
				</p>
			</div>
		</section>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">它到底帮你解决啥麻烦</h2>
			<div class="mt-4 space-y-4 leading-8" style="color: var(--muted);">
				<p>官方这页重点说了三件实在事：</p>
				<p>1. 活继续在你本机上跑，所以你本地文件、MCP、命令行工具、项目配置都还能照用。</p>
				<p>2. 终端、网页、手机能同步聊天。你在电脑上交代一半，出门拿手机还能接着说。</p>
				<p>3. 短暂断网、电脑睡眠后还能自动续上，不是稍微断一下就整锅端掉。</p>
			</div>
		</section>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">先决条件，别还没点火就上路</h2>
			<div class="mt-4 space-y-4 leading-8">
				<p>官方写得很清楚，Remote Control 不是谁来都能直接开：</p>
				<p style="color: var(--muted);">1. 账号得是 Pro、Max、Team 或 Enterprise。Team 和 Enterprise 还得管理员先在后台打开 Claude Code。</p>
				<p style="color: var(--muted);">2. 先在终端里跑过 <code>claude</code>，并用 <code>/login</code> 登录过 claude.ai 账号。API key 不支持这个功能。</p>
				<p style="color: var(--muted);">3. 还得至少进过一次项目目录，确认过 workspace trust。相当于先跟系统说“这块地我认，放心让它下地”。</p>
			</div>
		</section>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">怎么开一个远程会话</h2>
			<div class="mt-4 space-y-5 leading-8">
				<p>官方给了两种开法：一种是直接新开，一种是把已经在跑的会话接出来。</p>
				<div class="rounded-[24px] border p-6" style="border-color: var(--line); background: var(--panel-strong);">
					<h3 class="font-semibold text-lg">法子一：直接新开</h3>
					<div class="mt-3 space-y-3" style="color: var(--muted);">
						<p>先切到项目目录，再跑：</p>
						<pre class="overflow-x-auto rounded-2xl px-4 py-4 text-sm leading-7" style="background: var(--ink); color: var(--paper);"><code>claude remote-control</code></pre>
						<p>终端会一直挂着，等你别的设备来连。它会给你一个会话网址，还能按空格弹出二维码，方便手机扫一扫。</p>
						<p>可用参数里，官方特别提了这几个：<code>--name</code> 给会话起名，<code>--verbose</code> 看详细日志，<code>--sandbox</code> 或 <code>--no-sandbox</code> 控制是否开沙箱。</p>
					</div>
				</div>
				<div class="rounded-[24px] border p-6" style="border-color: var(--line); background: var(--panel-strong);">
					<h3 class="font-semibold text-lg">法子二：把正在跑的会话接出来</h3>
					<div class="mt-3 space-y-3" style="color: var(--muted);">
						<p>如果你已经在 Claude Code 里面干活了，直接敲：</p>
						<pre class="overflow-x-auto rounded-2xl px-4 py-4 text-sm leading-7" style="background: var(--ink); color: var(--paper);"><code>/remote-control</code></pre>
						<p>也能顺手带名字：</p>
						<pre class="overflow-x-auto rounded-2xl px-4 py-4 text-sm leading-7" style="background: var(--ink); color: var(--paper);"><code>/remote-control My Project</code></pre>
						<p>这样会把当前聊天记录一起带上。你先前聊到哪、改到哪，远程连上后都还在。</p>
					</div>
				</div>
			</div>
		</section>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">怎么从别的设备接进来</h2>
			<div class="mt-4 space-y-4 leading-8" style="color: var(--muted);">
				<p>官方给了三条路，都不复杂：</p>
				<p>1. 直接打开终端里显示的那个 session URL，用浏览器进 `claude.ai/code`。</p>
				<p>2. 扫二维码，在 Claude 手机 app 里直接打开。</p>
				<p>3. 自己去 `claude.ai/code` 或 Claude app 的会话列表里按名字找。在线时会显示电脑图标和绿色状态点。</p>
				<p>如果你电脑上那个环境已经有活跃会话，系统会问你是接着旧的，还是再起一个新的。</p>
			</div>
		</section>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">想每次都默认能远程？可以</h2>
			<div class="mt-4 space-y-4 leading-8">
				<p>
					官方说，默认只有你明确跑 <code>claude remote-control</code> 或 <code>/remote-control</code> 时才会开远程。
				</p>
				<p style="color: var(--muted);">
					如果你想让每次会话都自动带上远程入口，就在 Claude Code 里跑 <code>/config</code>，把 <code>Enable Remote Control for all sessions</code> 设成
					<code>true</code>。
				</p>
				<p style="color: var(--muted);">但要记住，一份 Claude Code 实例一次只支持一个 remote session。你要是开了多个实例，就各管各的。</p>
			</div>
		</section>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">安全这块，官方怎么交代</h2>
			<div class="mt-4 space-y-4 leading-8">
				<p>这页有个关键点：你的机器不用对外开端口。</p>
				<p style="color: var(--muted);">
					官方写的是，本地 Claude Code 只会主动发出 HTTPS 请求，不会在你机器上开一个让外面直接打进来的洞。
				</p>
				<p style="color: var(--muted);">
					当你启动 Remote Control 后，它会向 Anthropic API 报到并轮询消息；你用别的设备连上来后，服务端只是帮你把网页或手机的消息转给本地会话，通信走的是 TLS。
				</p>
				<p style="color: var(--muted);">另外它用的是多份短期凭证，各管各的用途，过期也各自过期，不是一把万能钥匙到处通用。</p>
			</div>
		</section>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">它和网页版 Claude Code 不是一回事</h2>
			<div class="mt-4 space-y-4 leading-8">
				<p>这两样东西看着都在 `claude.ai/code` 里用，但底下跑活的位置不一样。</p>
				<p>Remote Control：活在你自己机器上跑，本地工具、本地文件、本地 MCP 都在。</p>
				<p>Claude Code on the web：活在 Anthropic 管的云机器上跑，适合没带电脑、本地没克隆仓库、或者想并行开多份活的时候。</p>
				<p style="color: var(--muted);">
					官方建议也很直白：你已经在本地干到一半了，又想换设备继续，就用 Remote Control；你想无本地准备就起活，或者多线程并行，就用 web 版。
				</p>
			</div>
		</section>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">现在有哪些限制</h2>
			<div class="mt-4 space-y-4 leading-8" style="color: var(--muted);">
				<p>1. 一次只能连一个 remote session。</p>
				<p>2. 终端不能关。因为活就是在本地进程里跑，你把终端关了，会话也就没了。</p>
				<p>3. 如果机器醒着但长时间没网，官方写的是大约 10 分钟后会超时退出，到时得重新开。</p>
			</div>
		</section>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">这页给你的实际建议</h2>
			<div class="mt-4 space-y-4 leading-8">
				<p>如果你的工作强依赖本地环境，比如本地数据库、私有工具、MCP、没上传的代码，那就优先用 Remote Control。</p>
				<p>如果你只是想随手丢个明确任务出去，让它在云上慢慢跑，自己去忙别的，那更适合看下一页网页版。</p>
				<p>
					官方相关页：
					<a href="/docs/en/claude-code-on-the-web" class="underline decoration-dotted underline-offset-4">Claude Code on the web</a>
					。
				</p>
			</div>
		</section>
	</article>
</Layout>
