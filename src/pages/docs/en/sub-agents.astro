---
import Layout from '../../../layouts/Layout.astro';
import { getDocBySlug } from '../../../data/navigation';

const doc = getDocBySlug('sub-agents');

if (!doc) {
	throw new Error('Sub-agents doc metadata is missing');
}
---

<Layout title={`${doc.title} | 通俗版 Claude Code 文档`} description={doc.summary} pathname={doc.path}>
	<article class="space-y-8">
		<header class="card rounded-[32px] p-8 shadow-card">
			<p class="text-sm font-semibold uppercase tracking-[0.24em]" style="color: var(--muted);">Build with Claude Code</p>
			<h1 class="mt-3 font-display text-4xl font-semibold">Sub-agents 创建自定义子代理</h1>
			<p class="mt-4 max-w-3xl text-lg leading-8">{doc.summary}</p>
			<p class="mt-4 max-w-4xl leading-8" style="color: var(--muted);">
				原页来源：
				<a href="https://code.claude.com/docs/en/sub-agents" target="_blank" rel="noreferrer" class="underline decoration-dotted underline-offset-4">官方 Sub-agents</a>
			</p>
		</header>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">一句人话</h2>
			<div class="mt-4 space-y-4 leading-8">
				<p>子代理就是你养的一帮"小徒弟"，每个专门干一类活。遇到匹配的活，大师傅 Claude 就自动派给对应的小徒弟去干。</p>
				<p>比如你养个"代码审查员"，专门看代码；养个"测试编写员"，专门写单元测试。各管一摊，互不搅和。</p>
			</div>
		</section>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">为啥要用子代理</h2>
			<div class="mt-4 space-y-4 leading-8">
				<p><strong>1. 保持上下文干净</strong> — 探索代码的活交给子代理干，主对话里不掺和那些中间过程。</p>
				<p><strong>2. 限制权限</strong> — 有些子代理只给只读权限，它想改文件也改不了。</p>
				<p><strong>3. 复用配置</strong> — 配置好的子代理可以在所有项目里用，不用每次都重新设置。</p>
				<p><strong>4. 专业化</strong> — 每个子代理有自己的系统提示，干特定活更专业。</p>
				<p><strong>5. 控制成本</strong> — 简单活派给便宜的 Haiku 模型，复杂活才用 Sonnet 或 Opus。</p>
			</div>
		</section>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">内置的子代理</h2>
			<div class="mt-4 space-y-5 leading-8">
				<div class="rounded-[24px] border p-6" style="border-color: var(--line); background: var(--panel-strong);">
					<h3 class="text-lg font-semibold">Explore（探索者）</h3>
					<p class="mt-3" style="color: var(--muted);">只读模式，专门用来搜索和分析代码库。模型用 Haiku（快、便宜），不能改文件。适合查代码、找文件、理解项目结构。</p>
				</div>
				<div class="rounded-[24px] border p-6" style="border-color: var(--line); background: var(--panel-strong);">
					<h3 class="text-lg font-semibold">Plan（规划师）</h3>
					<p class="mt-3" style="color: var(--muted);">帮你制定计划，不直接动手。适合复杂任务前先理清楚步骤。</p>
				</div>
				<div class="rounded-[24px] border p-6" style="border-color: var(--line); background: var(--panel-strong);">
					<h3 class="text-lg font-semibold">General-purpose（通用型）</h3>
					<p class="mt-3" style="color: var(--muted);">啥都能干的默认代理，继承主对话的所有权限和工具。</p>
				</div>
			</div>
		</section>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">快速开始：创建第一个子代理</h2>
			<div class="mt-4 space-y-4 leading-8">
				<p>在 Claude Code 里运行 <code class="px-2 py-1 rounded bg-code">/agents</code> 命令，然后：</p>
				<ol class="list-decimal list-inside space-y-3 mt-4" style="color: var(--muted);">
					<li>选"Create new agent"（创建新代理）</li>
					<li>选"User-level"（用户级，所有项目都能用）或"Project-level"（只限当前项目）</li>
					<li>描述你要的代理，比如："一个代码审查员，专门看代码质量、安全性、最佳实践"</li>
					<li>选择工具权限（审查员就只给只读工具）</li>
					<li>选择模型（审查用 Sonnet 就够了）</li>
					<li>选个颜色（方便 UI 里识别）</li>
					<li>保存，立刻就能用</li>
				</ol>
			</div>
		</section>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">子代理文件长啥样</h2>
			<div class="mt-4 space-y-4 leading-8">
				<p>子代理是 Markdown 文件，带 YAML frontmatter。放在 <code class="px-2 py-1 rounded bg-code">~/.claude/agents/</code>（用户级）或 <code class="px-2 py-1 rounded bg-code">.claude/agents/</code>（项目级）。</p>
				<div class="mt-4 rounded-[24px] border p-6 font-mono text-sm overflow-x-auto" style="border-color: var(--line); background: var(--panel-strong);">
<pre>---
name: code-reviewer
description: 审查代码质量和最佳实践
tools: Read, Glob, Grep
model: sonnet
---

你是一个代码审查员。分析代码并提供具体、可操作的改进建议，
关注代码质量、安全性和最佳实践。</pre>
				</div>
			</div>
		</section>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">配置选项</h2>
			<div class="mt-4 space-y-4 leading-8" style="color: var(--muted);">
				<p><strong>name</strong>（必填）— 唯一标识，小写字母加连字符</p>
				<p><strong>description</strong>（必填）— 什么时候用这个代理，Claude 靠这个判断</p>
				<p><strong>tools</strong> — 能用的工具列表，不写就继承全部</p>
				<p><strong>model</strong> — 用哪个模型：sonnet、opus、haiku，或 inherit（继承主对话）</p>
				<p><strong>permissionMode</strong> — 权限模式：default、acceptEdits、dontAsk、bypassPermissions、plan</p>
				<p><strong>skills</strong> — 预加载的技能列表</p>
				<p><strong>memory</strong> — 持久记忆范围：user、project、local</p>
				<p><strong>hooks</strong> — 生命周期钩子</p>
			</div>
		</section>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">持久记忆</h2>
			<div class="mt-4 space-y-4 leading-8">
				<p>给子代理开启记忆，它能记住跨对话的经验和模式。配置 <code class="px-2 py-1 rounded bg-code">memory: user</code> 或 <code class="px-2 py-1 rounded bg-code">memory: project</code>。</p>
				<p style="color: var(--muted);">比如代码审查员可以记住你项目的编码习惯，下次审查时就能用这些知识。</p>
			</div>
		</section>

		<section class="card rounded-[32px] p-8 shadow-card">
			<h2 class="font-display text-2xl font-semibold">最后一句土话</h2>
			<div class="mt-4 space-y-4 leading-8">
				<p>子代理就是把大活拆成小活，每个小活养个专门干的人。用好了，效率翻倍；用乱了，一群帮工互相绊脚。</p>
				<p style="color: var(--muted);">建议从一两个简单子代理开始，慢慢摸索出适合你的分工方式。</p>
			</div>
		</section>
	</article>
</Layout>
